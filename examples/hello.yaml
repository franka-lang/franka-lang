# Hello World Example
# A simple Franka module that returns a greeting

module:
  name: "Hello World"
  description: "A simple greeting module"

functions:
  main:
    description: "Returns a greeting"
    logic: "Hello, Franka!"
